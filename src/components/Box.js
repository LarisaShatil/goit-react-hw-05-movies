import styled from 'styled-components';

import {
  color,
  space,
  layout,
  grid,
  background,
  border,
  height,
} from 'styled-system';

export const Box = styled('div')(
  color,
  space,
  layout,
  grid,
  background,
  border,
  height,
);